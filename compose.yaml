services:
  marketmafioso:
    build:
      context: .
      dockerfile: Dockerfile
    image: marketmafioso-server:local
    restart: unless-stopped
    ports:
      - "5088:8080"
    env_file:
      - ./marketmafioso.env
    volumes:
      - ./data/marketmafioso:/data
